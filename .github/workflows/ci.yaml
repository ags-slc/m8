name: CI

on:
  push:
    branches: [main]
  # No branch filter: restricting to [main] meant a pull request based on
  # another branch — a stacked PR — matched nothing and ran no checks at all,
  # silently, which looks identical to a PR whose checks all passed.
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          # go.mod is the single source of truth for the Go version.
          go-version-file: go.mod

      - name: Build
        run: go build ./...

      # We publish darwin/linux/windows on amd64 and arm64; make sure they all
      # still compile before a tag finds out for us.
      - name: Cross-compile release targets
        run: |
          for target in linux/amd64 linux/arm64 darwin/amd64 darwin/arm64 windows/amd64 windows/arm64; do
            echo "==> $target"
            GOOS="${target%/*}" GOARCH="${target#*/}" CGO_ENABLED=0 go build ./...
          done

      - name: Unit tests
        run: go test ./internal/parser/... ./internal/migration/... ./internal/schema/... ./internal/config/... -v -count=1

      # -p 1: these two packages each spin up PostgreSQL containers; running them
      # in parallel has produced intermittent failures.
      - name: Integration tests
        run: go test ./internal/engine/... ./internal/dump/... -v -count=1 -p 1 -timeout 600s

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          # go.mod is the single source of truth for the Go version.
          go-version-file: go.mod

      # golangci-lint v2 requires action v7 or later: on v6 the job died at
      # install ("golangci-lint v2 is not supported by golangci-lint-action v6")
      # and never linted anything, which is how the backlog accumulated.
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.13.1
